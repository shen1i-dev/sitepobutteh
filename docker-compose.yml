version: '3.8'
services:
  web:
    build: .
    env_file:
      - .env
    volumes:
      - ./:/app:rw
      - ./data:/app/data:rw
    # Ports and command are defined in docker-compose.override.yml for development
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
