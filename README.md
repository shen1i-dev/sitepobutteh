# Лабораторна робота №5: Розробка RESTful API

## Інформація про проєкт
- **Назва проєкту:** Магазин Техніки
- **Автори:** Владислав Кравчук 
- **Репозиторій:** https://github.com/shen1i-dev/sitepobutteh

---

## 📋 Опис проєкту

REST API для інтернет-магазину техніки, який дозволяє управляти товарами, замовленнями, відгуками та користувачами. API побудований на Flask з використанням SQLite як бази даних. Додатково реалізована красива веб-сторінка для перегляду товарів з API.

**Основні можливості:**
- ✅ CRUD операції для товарів (Create, Read, Update, Delete)
- ✅ Управління замовленнями та їх статусами
- ✅ Система відгуків користувачів
- ✅ Управління користувачами
- ✅ Автоматична документація через Swagger (Flasgger)
- ✅ JSON формат для всіх відповідей
- ✅ Обробка помилок з відповідними HTTP кодами

---

## 🛠️ Технології

| Технологія | Версія | Призначення |
|-----------|--------|------------|
| **Python** | 3.14+ | Мова програмування |
| **Flask** | 2.3.0 | Web framework |
| **Flask-SQLAlchemy** | 3.0.5 | ORM для бази даних |
| **SQLite3** | - | Реляційна база даних |
| **Flasgger** | 0.9.7.1 | Документація API (Swagger) |
| **Requests** | 2.x | HTTP клієнт для тестування |

---

## 📁 Структура проєкту

```
site/
├── app.py                          # Основний Flask додаток
├── models.py                       # Функції роботи з базою даних
├── requirements.txt                # Залежності проекту
├── db.sqlite                       # SQLite база даних
├── .github/
│   └── copilot-instructions.md     # Інструкції для AI агентів
├── routes/
│   ├── __init__.py
│   ├── shop.py                     # Маршрути магазину
│   ├── accounts.py                 # Автентифікація
│   ├── feedback.py                 # Відгуки
│   ├── admin.py                    # Адмін панель
│   └── api/                        # REST API endpoints
│       ├── __init__.py
│       ├── errors.py               # Обробка помилок
│       ├── products.py             # API для товарів
│       ├── orders.py               # API для замовлень
│       ├── feedback.py             # API для відгуків
│       └── users.py                # API для користувачів
├── templates/
│   ├── base.html                   # Базовий шаблон
│   ├── home.html                   # Головна сторінка
│   ├── shop.html                   # Каталог товарів
│   ├── cart.html                   # Кошик покупок
│   ├── feedback.html               # Форма відгуків
│   ├── accounts.html               # Облік користувачів
│   ├── admin.html                  # Адмін панель
│   ├── api_products.html           # Сторінка з товарами (API)
│   └── order_details.html          # Деталі замовлення
└── README.md                       # Цей файл
```

---

## 🚀 Запуск проєкту

### 1. Встановлення залежностей
```bash
pip install -r requirements.txt
```

### 2. Запуск Flask додатку
```bash
python app.py
```

### 3. Доступ до додатку
- **Головна сторінка:** http://127.0.0.1:5000/
- **API товарів (веб):** http://127.0.0.1:5000/api-products
- **Swagger документація:** http://127.0.0.1:5000/apidocs/

---

## 📡 REST API Endpoints

### 🛍️ Товари (Products)

#### 1. Отримати всі товари
- **URL:** `/api/v1/products`
- **Метод:** `GET`
- **Опис:** Отримує список всіх товарів з бази даних
- **Параметри:** 
  - `category` (опціонально) - фільтрування по категорії
  - `sort` (опціонально) - поле для сортування
  - `page` (опціонально) - номер сторінки

**Приклад запиту:**
```bash
curl http://127.0.0.1:5000/api/v1/products
```

**Приклад відповіді (200 OK):**
```json
{
  "status": "success",
  "data": [
    {
      "id": 25,
      "name": "Пральна машинка",
      "price": 6999.99,
      "image": "https://cdn.example.com/washing-machine.jpg"
    },
    {
      "id": 26,
      "name": "Телевізор",
      "price": 2999.99,
      "image": "https://cdn.example.com/tv.jpg"
    }
  ],
  "count": 2
}
```

---

#### 2. Отримати товар за ID
- **URL:** `/api/v1/products/<id>`
- **Метод:** `GET`
- **Опис:** Отримує деталі конкретного товару

**Приклад запиту:**
```bash
curl http://127.0.0.1:5000/api/v1/products/25
```

**Приклад відповіді (200 OK):**
```json
{
  "status": "success",
  "data": {
    "id": 25,
    "name": "Пральна машинка",
    "price": 6999.99,
    "image": "https://cdn.example.com/washing-machine.jpg"
  }
}
```

**Відповідь при помилці (404 Not Found):**
```json
{
  "status": "error",
  "message": "Product not found",
  "code": 404
}
```

---

#### 3. Створити товар
- **URL:** `/api/v1/products`
- **Метод:** `POST`
- **Опис:** Додає новий товар до магазину
- **Обов'язкові поля:** `name`, `price`
- **Опціональні поля:** `image`

**Приклад запиту:**
```bash
curl -X POST http://127.0.0.1:5000/api/v1/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Утюг",
    "price": 8999.99,
    "image": "https://cdn.example.com/iron.jpg"
  }'
```

**Приклад відповіді (201 Created):**
```json
{
  "status": "success",
  "message": "Product created",
  "data": {
    "id": 33
  }
}
```

---

#### 4. Оновити товар
- **URL:** `/api/v1/products/<id>`
- **Метод:** `PUT`
- **Опис:** Оновлює інформацію про товар

**Приклад запиту:**
```bash
curl -X PUT http://127.0.0.1:5000/api/v1/products/33 \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Утюг Pro",
    "price": 9999.99
  }'
```

**Приклад відповіді (200 OK):**
```json
{
  "status": "success",
  "message": "Product updated"
}
```

---

#### 5. Видалити товар
- **URL:** `/api/v1/products/<id>`
- **Метод:** `DELETE`
- **Опис:** Видаляє товар з магазину

**Приклад запиту:**
```bash
curl -X DELETE http://127.0.0.1:5000/api/v1/products/33
```

**Приклад відповіді (204 No Content):**
```
[Порожна відповідь]
```

---

### 📦 Замовлення (Orders)

#### 1. Отримати всі замовлення
- **URL:** `/api/v1/orders`
- **Метод:** `GET`
- **Опис:** Отримує список усіх замовлень

**Приклад запиту:**
```bash
curl http://127.0.0.1:5000/api/v1/orders
```

**Приклад відповіді (200 OK):**
```json
{
  "status": "success",
  "data": [
    {
      "id": 1,
      "email": "customer@example.com",
      "address": "вул. Шевченка, 1",
      "total_amount": 15999.98,
      "status": "pending",
      "date": "2025-12-08 10:30:00"
    }
  ]
}
```

---

#### 2. Отримати замовлення за ID
- **URL:** `/api/v1/orders/<id>`
- **Метод:** `GET`
- **Опис:** Отримує деталі конкретного замовлення

**Приклад запиту:**
```bash
curl http://127.0.0.1:5000/api/v1/orders/1
```

---

#### 3. Створити замовлення
- **URL:** `/api/v1/orders`
- **Метод:** `POST`
- **Опис:** Створює нове замовлення

**Приклад запиту:**
```bash
curl -X POST http://127.0.0.1:5000/api/v1/orders \
  -H "Content-Type: application/json" \
  -d '{
    "email": "customer@example.com",
    "address": "вул. Шевченка, 1",
    "total_price": 15999.98
  }'
```

**Приклад відповіді (201 Created):**
```json
{
  "status": "success",
  "message": "Order created",
  "data": {
    "id": 2
  }
}
```

---

### 💬 Відгуки (Feedback)

#### 1. Отримати всі відгуки
- **URL:** `/api/v1/feedback`
- **Метод:** `GET`
- **Опис:** Отримує список усіх відгуків користувачів

**Приклад запиту:**
```bash
curl http://127.0.0.1:5000/api/v1/feedback
```

**Приклад відповіді (200 OK):**
```json
{
  "status": "success",
  "data": [
    {
      "id": 1,
      "name": "Іван",
      "email": "ivan@example.com",
      "message": "Чудовий магазин!",
      "rating": 5
    }
  ]
}
```

---

#### 2. Додати відгук
- **URL:** `/api/v1/feedback`
- **Метод:** `POST`
- **Опис:** Додає новий відгук від користувача

**Приклад запиту:**
```bash
curl -X POST http://127.0.0.1:5000/api/v1/feedback \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Петро",
    "email": "petro@example.com",
    "message": "Дуже задоволений покупкою!",
    "rating": 4
  }'
```

**Приклад відповіді (201 Created):**
```json
{
  "status": "success",
  "message": "Feedback created",
  "data": {
    "id": 2
  }
}
```

---

### 👥 Користувачі (Users)

#### 1. Отримати всіх користувачів
- **URL:** `/api/v1/users`
- **Метод:** `GET`
- **Опис:** Отримує список користувачів

**Приклад запиту:**
```bash
curl http://127.0.0.1:5000/api/v1/users
```

---

#### 2. Створити користувача
- **URL:** `/api/v1/users`
- **Метод:** `POST`
- **Опис:** Реєструє нового користувача

**Приклад запиту:**
```bash
curl -X POST http://127.0.0.1:5000/api/v1/users \
  -H "Content-Type: application/json" \
  -d '{
    "email": "newuser@example.com",
    "password": "password123"
  }'
```

---

## ✅ Результати тестування

### Тестовий сценарій 1: Отримання всіх товарів
- **Мета:** Перевірити чи API повертає список товарів
- **Метод:** GET /api/v1/products
- **Результат:** ✅ **Успішно** (Status 200)
- **Деталі:**
  - Кількість товарів: 9
  - Товари містять ID, name, price, image
  - Відповідь у форматі JSON

---

### Тестовий сценарій 2: Отримання товару за ID
- **Мета:** Перевірити отримання конкретного товару
- **Метод:** GET /api/v1/products/25
- **Результат:** ✅ **Успішно** (Status 200)
- **Деталі:**
  - Товар: "Пральна машинка"
  - Ціна: 6999.99
  - Об'єкт містить усі потрібні поля

---

### Тестовий сценарій 3: Створення нового товару
- **Мета:** Перевірити додавання товару через API
- **Метод:** POST /api/v1/products
- **Результат:** ✅ **Успішно** (Status 201)
- **Деталі:**
  - Товар "Утюг" успішно додано
  - Отримано ID 33
  - Товар збережено в БД

---

### Тестовий сценарій 4: Оновлення товару
- **Мета:** Перевірити зміну даних товару
- **Метод:** PUT /api/v1/products/33
- **Результат:** ✅ **Успішно** (Status 200)
- **Деталі:**
  - Назва змінена з "Смартфон" на "Утюг"
  - Дані в БД оновлені
  - Зміни видно в GET запиті

---

### Тестовий сценарій 5: Видалення товару
- **Мета:** Перевірити видалення товару
- **Метод:** DELETE /api/v1/products/<id>
- **Результат:** ✅ **Успішно** (Status 204)
- **Деталі:**
  - Товар видалено з БД
  - Повернена порожна відповідь
  - GET запит на видалений товар повертає 404

---

### Тестовий сценарій 6: Обробка помилок
- **Мета:** Перевірити помилки валідації
- **Метод:** POST /api/v1/products (без обов'язкових полів)
- **Результат:** ✅ **Успішно** (Status 400)
- **Деталі:**
  ```json
  {
    "status": "error",
    "message": "Missing required fields"
  }
  ```

---

### Тестовий сценарій 7: Swagger документація
- **Мета:** Перевірити автоматичну документацію
- **URL:** GET /apidocs/
- **Результат:** ✅ **Успішно** (Status 200)
- **Деталі:**
  - Доступна інтерактивна Swagger UI
  - Показані всі endpoints
  - Можна тестувати з браузера

---

## ❌ Обробка помилок

| Код | Статус | Коли виникає | Приклад |
|-----|--------|-------------|---------|
| **400** | Bad Request | Невірний формат запиту або відсутні обов'язкові поля | Не передано `name` або `price` при створенні товару |
| **404** | Not Found | Ресурс не знайдено (товар з ID не існує) | GET /api/v1/products/999 |
| **500** | Internal Server Error | Помилка на сервері (проблеми з БД, невідловлене виняток) | Невалідні дані в запиті |

### Формат помилки:
```json
{
  "status": "error",
  "message": "Описання помилки",
  "code": 400
}
```

---

## 🌐 Веб-інтерфейс

Крім REST API, проект також має красивий веб-інтерфейс:

### Сторінка API Товарів
- **URL:** http://127.0.0.1:5000/api-products
- **Функціональність:**
  - Динамічне завантаження товарів з API
  - Сітка карточок товарів
  - Статистика (кількість, середня ціна)
  - Кнопки "Додати в кошик" та "Деталі"
  - Адаптивний дизайн для мобільних
  - Анімації при наведенні

### Навігаційне меню
Посилання на API сторінку додано у верхнє меню:
```
Головна | Магазин | 🔍 API Товари | Кошик | Про нас | Зворотній зв'язок | Аккаунт
```

---

## 📚 Документація

### Swagger UI
Інтерактивна документація доступна на:
- **URL:** http://127.0.0.1:5000/apidocs/
- **Функції:**
  - Перегляд усіх endpoints
  - Схеми запитів та відповідей
  - Можливість тестування прямо з браузера
  - Автоматично генерується з docstrings

### GitHub Repository
- **URL:** https://github.com/shen1i-dev/sitepobutteh
- **Branch:** master
- **Останній коміт:** Lab 5: Add REST API endpoints and API Products page

---

## 🎯 Висновки

### Що вдалося реалізувати:

✅ **Основне завдання (Рівень 1):**
- Розроблено базовий REST API з 6+ endpoints
- Реалізована обробка помилок з HTTP кодами
- JSON формат для всіх даних
- Тестування в Postman та браузері

✅ **Додаткове завдання (Рівень 2):**
- Розроблена документація через Flasgger (Swagger)
- Описані всі endpoints зі схемами
- Приклади запитів та відповідей
- Опис обробки помилок

✅ **Розширена функціональність:**
- Веб-сторінка для перегляду товарів з API
- Красивий UI з градієнтами та анімаціями
- Навігаційне меню з посиланням на API
- Статистика товарів (кількість, мін/макс/середня ціна)
- Динамічне завантаження даних без перезавантаження сторінки

---

## 📝 Примітки для розробника

### Як розширювати API:
1. Створити нов файл в `routes/api/resource.py`
2. Імпортувати blueprint в `app.py`
3. Зареєструвати blueprint з префіксом `/api/v1/resource`
4. Додати docstring з `---` для Swagger документації

### Як додавати нові поля до товарів:
1. Оновити `init_db()` в `models.py`
2. Оновити `CREATE TABLE` SQL команду
3. Оновити функції CRUD операцій
4. Оновити API endpoints

### Тестування:
```bash
# Запуск Flask у debug режимі
python app.py

# Тестування в curl
curl http://127.0.0.1:5000/api/v1/products

# Тестування в Python
import requests
r = requests.get('http://127.0.0.1:5000/api/v1/products')
print(r.json())
```

---

## 📞 Контакти

- **GitHub:** https://github.com/shen1i-dev
- **Репозиторій:** https://github.com/shen1i-dev/sitepobutteh

---

**Останнє оновлення:** 8 грудня 2025
**Версія:** 1.0.0
